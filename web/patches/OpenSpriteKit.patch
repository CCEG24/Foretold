diff --git a/Sources/OpenSpriteKit/Internal/SKNullSceneRenderer.swift b/Sources/OpenSpriteKit/Internal/SKNullSceneRenderer.swift
index afa82f4..3e28de0 100644
--- a/Sources/OpenSpriteKit/Internal/SKNullSceneRenderer.swift
+++ b/Sources/OpenSpriteKit/Internal/SKNullSceneRenderer.swift
@@ -28,7 +28,7 @@ internal final class SKNullSceneRenderer: SKSceneRendererDelegate {
         .unsupportedPlatform
     }
 
-    func resize(width: Int, height: Int) {
+    func resize(width: Int, height: Int, contentsScale: CGFloat) {
         // No-op
     }
 
diff --git a/Sources/OpenSpriteKit/Internal/SKSceneRendererDelegate.swift b/Sources/OpenSpriteKit/Internal/SKSceneRendererDelegate.swift
index 65826bd..867a22c 100644
--- a/Sources/OpenSpriteKit/Internal/SKSceneRendererDelegate.swift
+++ b/Sources/OpenSpriteKit/Internal/SKSceneRendererDelegate.swift
@@ -53,9 +53,12 @@ internal protocol SKSceneRendererDelegate: AnyObject {
     /// depth buffers, render textures, etc.
     ///
     /// - Parameters:
-    ///   - width: The new width in pixels.
-    ///   - height: The new height in pixels.
-    func resize(width: Int, height: Int)
+    ///   - width: The new width in logical points.
+    ///   - height: The new height in logical points.
+    ///   - contentsScale: Device pixels per point. Backends that render to a
+    ///     HiDPI surface allocate `width × height × contentsScale²` pixels and
+    ///     keep the scene's coordinates in points.
+    func resize(width: Int, height: Int, contentsScale: CGFloat)
 
     // MARK: - Cleanup
 
@@ -65,3 +68,14 @@ internal protocol SKSceneRendererDelegate: AnyObject {
     /// any further rendering operations.
     func invalidate()
 }
+
+extension SKSceneRendererDelegate {
+
+    /// Resizes at 1× — points and pixels are the same thing.
+    ///
+    /// Swift forbids default arguments on a protocol requirement, so the
+    /// unscaled spelling every existing caller uses lives here instead.
+    func resize(width: Int, height: Int) {
+        resize(width: width, height: height, contentsScale: 1)
+    }
+}
diff --git a/Sources/OpenSpriteKit/Internal/SKViewRenderer.swift b/Sources/OpenSpriteKit/Internal/SKViewRenderer.swift
index bef501f..15e69d1 100644
--- a/Sources/OpenSpriteKit/Internal/SKViewRenderer.swift
+++ b/Sources/OpenSpriteKit/Internal/SKViewRenderer.swift
@@ -512,9 +512,10 @@ internal final class SKViewRenderer: CADisplayLinkDelegate {
     /// Updates the renderer size when the view is resized.
     ///
     /// - Parameters:
-    ///   - width: The new width in pixels.
-    ///   - height: The new height in pixels.
-    func resize(width: Int, height: Int) {
-        rendererDelegate.resize(width: width, height: height)
+    ///   - width: The new width in logical points.
+    ///   - height: The new height in logical points.
+    ///   - contentsScale: Device pixels per point (see `SKRenderer.resize`).
+    func resize(width: Int, height: Int, contentsScale: CGFloat = 1) {
+        rendererDelegate.resize(width: width, height: height, contentsScale: contentsScale)
     }
 }
diff --git a/Sources/OpenSpriteKit/Internal/SKWebGPUSceneRenderer.swift b/Sources/OpenSpriteKit/Internal/SKWebGPUSceneRenderer.swift
index eb82116..a11ad6f 100644
--- a/Sources/OpenSpriteKit/Internal/SKWebGPUSceneRenderer.swift
+++ b/Sources/OpenSpriteKit/Internal/SKWebGPUSceneRenderer.swift
@@ -57,8 +57,8 @@ internal final class SKWebGPUSceneRenderer: SKSceneRendererDelegate {
         return nil
     }
 
-    func resize(width: Int, height: Int) {
-        webGPURenderer?.resize(width: width, height: height)
+    func resize(width: Int, height: Int, contentsScale: CGFloat) {
+        webGPURenderer?.resize(width: width, height: height, contentsScale: contentsScale)
     }
 
     func invalidate() {
diff --git a/Sources/OpenSpriteKit/SKLabelNode.swift b/Sources/OpenSpriteKit/SKLabelNode.swift
index dda47e3..7c69753 100644
--- a/Sources/OpenSpriteKit/SKLabelNode.swift
+++ b/Sources/OpenSpriteKit/SKLabelNode.swift
@@ -72,6 +72,11 @@ open class SKLabelNode: SKNode {
             #else
             textLayer.font = fontName
             #endif
+            // The font changes the measured text size, so the layer bounds have
+            // to be recomputed — without this, bounds kept whatever font was set
+            // when `text`/`fontSize` last ran (i.e. only correct if fontName
+            // happens to be assigned before them).
+            updateLayerBounds()
         }
     }
 
@@ -92,25 +97,57 @@ open class SKLabelNode: SKNode {
     /// This method only sets bounds width when preferredMaxLayoutWidth is specified
     /// (for multi-line text wrapping).
     private func updateLayerBounds() {
+        // NOTE: The renderer (CAWebGPURenderer.renderText) is documented to
+        // auto-measure zero-bounds text, but in practice it bails when
+        // bounds.size is zero — so nothing draws. Give the layer a concrete,
+        // non-zero size from the estimate; a small horizontal pad avoids
+        // right-edge clipping when the 0.6·fontSize heuristic under-measures.
+        let estimate = estimatedTextSize()
+        guard estimate.width > 0, estimate.height > 0 else {
+            layer.bounds = .zero
+            return
+        }
         if preferredMaxLayoutWidth > 0 {
-            // Set width for text wrapping - height will be calculated by renderer
-            layer.bounds = CGRect(x: 0, y: 0, width: preferredMaxLayoutWidth, height: 0)
+            layer.bounds = CGRect(x: 0, y: 0, width: preferredMaxLayoutWidth, height: estimate.height)
         } else {
-            // Let renderer auto-measure the text size
-            layer.bounds = .zero
+            layer.bounds = CGRect(x: 0, y: 0, width: estimate.width + fontSize * 0.5, height: estimate.height)
         }
     }
 
     // MARK: - Alignment Properties
 
     /// The vertical position of the text within the node.
-    open var verticalAlignmentMode: SKLabelVerticalAlignmentMode = .baseline
+    open var verticalAlignmentMode: SKLabelVerticalAlignmentMode = .baseline {
+        didSet { updateAnchorForAlignment() }
+    }
 
     /// The horizontal position of the text within the node.
     open var horizontalAlignmentMode: SKLabelHorizontalAlignmentMode = .center {
         didSet {
             updateTextLayerAlignment()
+            updateAnchorForAlignment()
+        }
+    }
+
+    /// Maps the label's alignment modes onto the backing layer's anchorPoint so
+    /// text positions like SpriteKit. The base SKNode leaves anchorPoint at
+    /// (0.5, 0.5), which centers every label on its position — wrong for left/
+    /// right/top/bottom-aligned labels (e.g. left-aligned text ran off-screen).
+    private func updateAnchorForAlignment() {
+        let ax: CGFloat
+        switch horizontalAlignmentMode {
+        case .left: ax = 0.0
+        case .center: ax = 0.5
+        case .right: ax = 1.0
+        }
+        let ay: CGFloat
+        switch verticalAlignmentMode {
+        case .top: ay = 1.0
+        case .center: ay = 0.5
+        case .bottom: ay = 0.0
+        case .baseline: ay = 0.17   // baseline sits ~0.2·fontSize above the bottom
         }
+        layer.anchorPoint = CGPoint(x: ax, y: ay)
     }
 
     /// Updates the text layer alignment based on horizontal alignment mode.
@@ -172,31 +209,86 @@ open class SKLabelNode: SKNode {
 
     // MARK: - Computed Properties
 
-    /// Estimates the text size based on the current text and font settings.
+    /// The size of the label's text with the current font settings.
     ///
-    /// This is an approximation since actual text measurement requires platform-specific APIs.
-    /// The renderer may calculate the actual size for rendering purposes.
+    /// Prefers real Canvas2D metrics (`CATextMetrics`), measured with the exact
+    /// font string the renderer draws with, so the layer's bounds match what is
+    /// actually rasterized — mis-measuring here made the renderer truncate text
+    /// ("FORETOLD" → "foret…") and threw off any layout keyed off `frame`.
+    /// Falls back to a per-character approximation where measurement isn't
+    /// available (native test builds).
     private func estimatedTextSize() -> CGSize {
         guard let text = text, !text.isEmpty else {
             return .zero
         }
 
-        // Approximate character width as 0.6 * fontSize for typical fonts
-        let characterWidthFactor: CGFloat = 0.6
         let lineHeight = fontSize * 1.2
+        let wraps = preferredMaxLayoutWidth > 0
+            && (lineBreakMode == .byWordWrapping || lineBreakMode == .byCharWrapping)
+
+        if let measured = CATextMetrics.measuredWidth(text, fontName: fontName, fontSize: fontSize) {
+            if wraps {
+                let lines = wrappedLineCount(text, maximumWidth: preferredMaxLayoutWidth, singleLineWidth: measured)
+                let effectiveLines = numberOfLines > 0 ? min(lines, numberOfLines) : lines
+                return CGSize(width: preferredMaxLayoutWidth, height: lineHeight * CGFloat(effectiveLines))
+            }
+            // Explicit line breaks still lay out as multiple lines even without
+            // a wrapping width; size to the widest of them.
+            let paragraphs = text.split(separator: "\n", omittingEmptySubsequences: false)
+            if paragraphs.count > 1 {
+                let widest = paragraphs
+                    .map { CATextMetrics.measuredWidth(String($0), fontName: fontName, fontSize: fontSize) ?? 0 }
+                    .max() ?? measured
+                return CGSize(
+                    width: widest + fontSize * 0.1,
+                    height: lineHeight * CGFloat(paragraphs.count)
+                )
+            }
+            // A sliver of padding keeps antialiased edges from clipping.
+            return CGSize(width: measured + fontSize * 0.1, height: lineHeight)
+        }
 
-        if preferredMaxLayoutWidth > 0 && (lineBreakMode == .byWordWrapping || lineBreakMode == .byCharWrapping) {
-            // Multi-line text: estimate based on wrapping
+        // Fallback: approximate character width (no measurement available).
+        let characterWidthFactor: CGFloat = 0.75
+        if wraps {
             let charWidth = fontSize * characterWidthFactor
             let charsPerLine = max(1, Int(preferredMaxLayoutWidth / charWidth))
             let lines = max(1, (text.count + charsPerLine - 1) / charsPerLine)
             let effectiveLines = numberOfLines > 0 ? min(lines, numberOfLines) : lines
             return CGSize(width: preferredMaxLayoutWidth, height: lineHeight * CGFloat(effectiveLines))
-        } else {
-            // Single line text
-            let width = CGFloat(text.count) * fontSize * characterWidthFactor
-            return CGSize(width: width, height: lineHeight)
         }
+        let width = CGFloat(text.count) * fontSize * characterWidthFactor
+        return CGSize(width: width, height: lineHeight)
+    }
+
+    /// Greedy word wrap using measured widths, mirroring the renderer's layout so
+    /// the reserved height matches the number of lines actually drawn.
+    private func wrappedLineCount(
+        _ text: String,
+        maximumWidth: CGFloat,
+        singleLineWidth: CGFloat
+    ) -> Int {
+        let paragraphs = text.split(separator: "\n", omittingEmptySubsequences: false)
+        // Fits on one line and has no explicit breaks — nothing to wrap.
+        if singleLineWidth <= maximumWidth && paragraphs.count <= 1 { return 1 }
+
+        var total = 0
+        for paragraph in paragraphs {
+            var lines = 1
+            var current = ""
+            for word in paragraph.split(separator: " ") {
+                let candidate = current.isEmpty ? String(word) : current + " " + String(word)
+                let width = CATextMetrics.measuredWidth(candidate, fontName: fontName, fontSize: fontSize) ?? 0
+                if width > maximumWidth && !current.isEmpty {
+                    lines += 1
+                    current = String(word)
+                } else {
+                    current = candidate
+                }
+            }
+            total += lines
+        }
+        return max(1, total)
     }
 
     /// A rectangle in the label's local coordinate system that defines its content area.
@@ -339,6 +431,7 @@ open class SKLabelNode: SKNode {
         #endif
         updateTextLayerAlignment()
         updateTextLayerTruncation()
+        updateAnchorForAlignment()
     }
 
     // MARK: - Copying
diff --git a/Sources/OpenSpriteKit/SKNode.swift b/Sources/OpenSpriteKit/SKNode.swift
index c4aa270..de5aadd 100644
--- a/Sources/OpenSpriteKit/SKNode.swift
+++ b/Sources/OpenSpriteKit/SKNode.swift
@@ -1211,8 +1211,12 @@ open class SKNode {
             depthCounter += 1
             let nodeZ = accZ + node.zPosition
 
-            // Add to results only if hittable (visible AND interactive AND at point)
-            if node.isUserInteractionEnabled && node._contentBounds.contains(point) {
+            // SpriteKit's nodes(at:) returns ALL visible nodes whose bounds contain
+            // the point — isUserInteractionEnabled gates event *delivery*, not this
+            // query. (Foretold does name-based hit-testing via nodes(at:).name and
+            // never sets isUserInteractionEnabled, so filtering on it broke every
+            // menu/button click.)
+            if node._contentBounds.contains(point) {
                 results.append((node, nodeZ, currentDepth))
             }
 
diff --git a/Sources/OpenSpriteKit/SKRenderer.swift b/Sources/OpenSpriteKit/SKRenderer.swift
index 978a4eb..2ba47ce 100644
--- a/Sources/OpenSpriteKit/SKRenderer.swift
+++ b/Sources/OpenSpriteKit/SKRenderer.swift
@@ -127,10 +127,14 @@ open class SKRenderer {
     /// Resizes the renderer to match a new canvas size.
     ///
     /// - Parameters:
-    ///   - width: The new width in pixels.
-    ///   - height: The new height in pixels.
-    public func resize(width: Int, height: Int) {
-        rendererDelegate.resize(width: width, height: height)
+    ///   - width: The new width in logical points.
+    ///   - height: The new height in logical points.
+    ///   - contentsScale: Device pixels per point — pass the page's
+    ///     `devicePixelRatio` to render at the display's true resolution. The
+    ///     scene keeps its coordinates in points, so nothing in the layer tree
+    ///     has to be rescaled; only the backing store grows.
+    public func resize(width: Int, height: Int, contentsScale: CGFloat = 1) {
+        rendererDelegate.resize(width: width, height: height, contentsScale: contentsScale)
     }
 
     // MARK: - Frame Cycle
