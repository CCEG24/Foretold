diff --git a/Sources/OpenCoreAnimation/CALayer.swift b/Sources/OpenCoreAnimation/CALayer.swift
index e1d55a9..8086a34 100644
--- a/Sources/OpenCoreAnimation/CALayer.swift
+++ b/Sources/OpenCoreAnimation/CALayer.swift
@@ -58,6 +58,8 @@ open class CALayer: CAMediaTiming, Hashable {
             self._contents = otherLayer._contents
             self._committedImageContentsStorage =
                 otherLayer._committedImageContentsStorage
+            // NOTE(foretold fork): carried with the contents it belongs to.
+            self._contentsStorageCache = otherLayer._contentsStorageCache
             self._contentsRect = otherLayer._contentsRect
             self._contentsCenter = otherLayer._contentsCenter
             self.contentsGravity = otherLayer.contentsGravity
@@ -323,6 +325,9 @@ open class CALayer: CAMediaTiming, Hashable {
         presentation._contents = _contents
         presentation._committedImageContentsStorage =
             _committedImageContentsStorage
+        // NOTE(foretold fork): the presentation layer is what gets snapshotted
+        // each frame, so without this the cache would never be consulted.
+        presentation._contentsStorageCache = _contentsStorageCache
         presentation._contentsRect = _contentsRect
         presentation._contentsCenter = _contentsCenter
         presentation.contentsGravity = contentsGravity
@@ -3621,6 +3626,12 @@ open class CALayer: CAMediaTiming, Hashable {
     /// Renderer-ready image storage owned by a committed animation evaluator.
     internal var _committedImageContentsStorage:
         CGImageTextureStorage?
+    /// NOTE(foretold fork): the last conversion of `contents` into
+    /// renderer-ready storage, kept so repeated snapshots of an unchanged
+    /// image reuse it — and reuse its `CacheIdentity`, which is what the
+    /// renderer's texture cache is keyed on. Cleared by the `contents` setter
+    /// below, so it can never outlive the image it was made from.
+    internal var _contentsStorageCache: CGImageTextureStorage?
     internal var _contentsAssignmentGeneration: UInt64 = 0
     open var contents: Any? {
         get { _contents }
@@ -3628,6 +3639,7 @@ open class CALayer: CAMediaTiming, Hashable {
             let oldValue = _contents
             _contents = newValue
             _committedImageContentsStorage = nil
+            _contentsStorageCache = nil
             _contentsAssignmentGeneration &+= 1
             delegateBackingStore = nil
             markDirty(.contents)
@@ -3937,7 +3949,11 @@ open class CALayer: CAMediaTiming, Hashable {
     }
 
     /// A Boolean indicating whether the layer is allowed to composite itself as a group separate from its parent.
-    open var allowsGroupOpacity: Bool = true {
+    // NOTE(foretold fork): defaulted to false — the offscreen group-opacity
+    // compositing path renders black in this WebGPU build, so any opacity < 1
+    // (every fade animation) blanked the frame. Simple per-layer alpha is fine
+    // for our 2D game and avoids that path entirely.
+    open var allowsGroupOpacity: Bool = false {
         didSet {
             guard oldValue != allowsGroupOpacity else { return }
             markDirty(.appearance)
diff --git a/Sources/OpenCoreAnimation/CARenderSnapshot.swift b/Sources/OpenCoreAnimation/CARenderSnapshot.swift
index 253953c..fc9878d 100644
--- a/Sources/OpenCoreAnimation/CARenderSnapshot.swift
+++ b/Sources/OpenCoreAnimation/CARenderSnapshot.swift
@@ -1337,6 +1337,11 @@ internal struct CARenderSnapshot: Sendable {
                     layer._committedImageContentsStorage {
             storage = committedStorage
             origin = .layerContents
+        } else if let cached = layer._contentsStorageCache {
+            // NOTE(foretold fork): see below — a snapshot of an unchanged
+            // image reuses the conversion, and with it its cache identity.
+            storage = cached
+            origin = .layerContents
         } else if let contents = layer.contents {
             guard let contentsImage = contents as? CGImage else {
                 throw .unsupportedContentsType(
@@ -1349,6 +1354,16 @@ internal struct CARenderSnapshot: Sendable {
             } catch {
                 throw .imageConversion(error)
             }
+            // NOTE(foretold fork): remember the conversion on the layer.
+            // Without this, every snapshot that isn't served by `staticValues`
+            // re-converts the CGImage and mints a fresh `CacheIdentity`, so
+            // the renderer's texture cache misses and re-uploads (and re-mips)
+            // every image on screen. One dirtied layer anywhere in the tree —
+            // a single tile changing colour — cost ~100ms of GPU work with
+            // only seven small sprites on the board. The `contents` setter
+            // clears this, so a layer that genuinely changes image still
+            // converts exactly once.
+            layer._contentsStorageCache = storage
             origin = .layerContents
         } else {
             return nil
diff --git a/Sources/OpenCoreAnimation/CAWebGPURenderer.swift b/Sources/OpenCoreAnimation/CAWebGPURenderer.swift
index 17840e5..e66bdf8 100644
--- a/Sources/OpenCoreAnimation/CAWebGPURenderer.swift
+++ b/Sources/OpenCoreAnimation/CAWebGPURenderer.swift
@@ -961,9 +961,27 @@ private final class CommittedAnimationEvaluationFailure {
     /// view alongside it and invalidate together with the texture.
     private var depthTextureView: GPUTextureView?
 
-    /// The current size.
+    /// The current size, in logical points.
+    ///
+    /// Every projection matrix, vertex position and layer bound is expressed in
+    /// this space. The backing store may be larger — see `contentsScale`.
     public private(set) var size: CGSize = CGSize(width: 0, height: 0)
 
+    /// How many device pixels one logical point occupies (HiDPI / retina).
+    ///
+    /// Set through `resize(width:height:contentsScale:)`. The canvas backing
+    /// store, the depth attachment, every render-pass viewport and scissor rect,
+    /// and text rasterization all work in `size × contentsScale` pixels, while
+    /// scene coordinates stay in points — so a 2× display renders sharp without
+    /// the caller rescaling its layer tree.
+    public private(set) var contentsScale: CGFloat = 1
+
+    /// The backing-store size in device pixels: `size × contentsScale`, rounded.
+    ///
+    /// Stored rather than recomputed so the canvas, the depth texture and the
+    /// viewport can never disagree by a rounding step.
+    public private(set) var pixelSize: CGSize = CGSize(width: 0, height: 0)
+
     /// Number of transition source textures captured during this renderer's lifetime.
     @_spi(RendererDiagnostics)
     public private(set) var transitionSourceCaptureCount: Int = 0
@@ -1478,8 +1496,14 @@ private final class CommittedAnimationEvaluationFailure {
     /// Overrides viewport-derived calculations while a layer subtree is captured offscreen.
     private var renderTargetSizeOverride: CGSize?
 
+    /// The render target's size in device pixels.
+    ///
+    /// Everything derived from this is measured in pixels, not points: scissor
+    /// rects (`setScissorRect` takes framebuffer pixels), pass viewports, and
+    /// the viewport-sized offscreen captures shadows and filters allocate. An
+    /// override is already a pixel size, so it passes through untouched.
     private var currentRenderTargetSize: CGSize {
-        renderTargetSizeOverride ?? size
+        renderTargetSizeOverride ?? pixelSize
     }
 
     /// Returns the current effective opacity from the stack.
@@ -2338,8 +2362,17 @@ private final class CommittedAnimationEvaluationFailure {
         }
         context = GPUCanvasContext(jsObject: ctxObject)
 
-        guard configureCanvas(toneMappingMode: .standard) else {
-            throw CARendererError.canvasNotConfigured
+        if !configureCanvas(toneMappingMode: .standard) {
+            // A float16 canvas is optional in practice: Firefox rejects one
+            // outright (bugzilla 1834395). Fall back to whatever format the
+            // browser prefers — every pipeline below targets `preferredFormat`
+            // and the readback path already handles both widths, so this is a
+            // complete configuration rather than a degraded one, just limited
+            // to standard dynamic range.
+            preferredFormat = gpu.preferredCanvasFormat
+            guard configureCanvas(toneMappingMode: .standard) else {
+                throw CARendererError.canvasNotConfigured
+            }
         }
 
         // Create shader module
@@ -2592,7 +2625,16 @@ private final class CommittedAnimationEvaluationFailure {
             maxMemoryBytes: 64 * 1024 * 1024  // 64 MB
         )
 
-        size = CGSize(width: renderTarget.width, height: renderTarget.height)
+        // The canvas element is measured in pixels, so the logical size is that
+        // divided by the scale. `contentsScale` is normally still its default 1
+        // here (a caller resizes after initializing), which makes this the
+        // identity it has always been; dividing matters only if someone resized
+        // to a HiDPI backing store before initializing.
+        pixelSize = CGSize(width: renderTarget.width, height: renderTarget.height)
+        size = CGSize(
+            width: CGFloat(renderTarget.width) / contentsScale,
+            height: CGFloat(renderTarget.height) / contentsScale
+        )
 
         // Create depth texture
         let depthResources = createDepthResources(
@@ -4239,12 +4281,20 @@ private final class CommittedAnimationEvaluationFailure {
     /// successful in that case.
     private func configureCanvas(toneMappingMode: GPUCanvasToneMappingMode) -> Bool {
         guard let device, let context else { return false }
-        context.configure(GPUCanvasConfiguration(
-            device: device,
-            format: preferredFormat,
-            usage: [.renderAttachment, .copySrc],
-            toneMapping: GPUCanvasToneMapping(mode: toneMappingMode)
-        ))
+        do {
+            try context.configureThrowing(GPUCanvasConfiguration(
+                device: device,
+                format: preferredFormat,
+                usage: [.renderAttachment, .copySrc],
+                toneMapping: GPUCanvasToneMapping(mode: toneMappingMode)
+            ))
+        } catch {
+            // A format the browser hasn't implemented arrives as a TypeError,
+            // and an uncaught JavaScript exception tears down the WebAssembly
+            // instance. Reporting it as "not configured" lets the caller retry
+            // with something narrower.
+            return false
+        }
 
         let reportedMode = context.getConfiguration()?.toneMapping?.mode
         supportsExtendedDynamicRangeOutput = reportedMode != nil
@@ -4504,10 +4554,30 @@ private final class CommittedAnimationEvaluationFailure {
         return true
     }
 
+    /// Resizes the render target at 1×, where points and pixels coincide.
+    ///
+    /// This is `CARendererDelegate`'s spelling. A protocol requirement can't be
+    /// satisfied by a method with a defaulted extra parameter, so it forwards
+    /// rather than sharing one signature with the HiDPI variant below.
     public func resize(width: Int, height: Int) {
+        resize(width: width, height: height, contentsScale: 1)
+    }
+
+    /// Resizes the render target.
+    ///
+    /// - Parameters:
+    ///   - width: The new width in logical points.
+    ///   - height: The new height in logical points.
+    ///   - contentsScale: Device pixels per point (pass the page's
+    ///     `devicePixelRatio` to render sharp on a HiDPI display). The backing
+    ///     store becomes `width × height × contentsScale²` pixels while the
+    ///     layer tree keeps working in points.
+    public func resize(width: Int, height: Int, contentsScale: CGFloat) {
         let maximumTextureDimension = device.map {
             Int($0.limits.maxTextureDimension2D)
         } ?? Int.max
+        // The logical target validates the point-space size that drives every
+        // projection matrix; `size` is set from it below.
         let renderTarget: CARenderTargetConfiguration
         do {
             renderTarget = try CARenderTargetConfiguration(
@@ -4519,6 +4589,29 @@ private final class CommittedAnimationEvaluationFailure {
             recordFrameRenderFailure(.invalidRenderTarget(error))
             return
         }
+        // And the backing target validates the pixel-space size, which is what
+        // actually has to fit in a texture. A scale that overflows the device
+        // limit is rejected outright rather than silently clamped: the caller
+        // picked it, and a half-applied scale renders at the wrong size.
+        guard contentsScale.isFinite, contentsScale > 0 else {
+            recordFrameRenderFailure(
+                .invalidRenderTarget(
+                    .invalidDimensions(width: Double(width), height: Double(height))
+                )
+            )
+            return
+        }
+        let backingTarget: CARenderTargetConfiguration
+        do {
+            backingTarget = try CARenderTargetConfiguration(
+                width: (Double(width) * Double(contentsScale)).rounded(),
+                height: (Double(height) * Double(contentsScale)).rounded(),
+                maximumTextureDimension: maximumTextureDimension
+            )
+        } catch {
+            recordFrameRenderFailure(.invalidRenderTarget(error))
+            return
+        }
 
         // Configure first so a rejected context change cannot leave the public
         // size and canvas dimensions partially committed.
@@ -4529,17 +4622,22 @@ private final class CommittedAnimationEvaluationFailure {
             return
         }
 
-        // Update canvas size
+        // Update canvas size. The element's backing store is in pixels; its CSS
+        // box (and so the scene's apparent size) is the page's business.
         size = CGSize(width: renderTarget.width, height: renderTarget.height)
-        canvas.width = .number(Double(renderTarget.width))
-        canvas.height = .number(Double(renderTarget.height))
-
-        // Recreate depth texture
+        self.contentsScale = contentsScale
+        pixelSize = CGSize(width: backingTarget.width, height: backingTarget.height)
+        canvas.width = .number(Double(backingTarget.width))
+        canvas.height = .number(Double(backingTarget.height))
+
+        // Recreate depth texture. It's a companion attachment to the canvas
+        // texture, so it's sized in pixels — every pass that pairs the two has
+        // to agree or WebGPU rejects the descriptor.
         if let device {
             let depthResources = createDepthResources(
                 device: device,
-                width: renderTarget.width,
-                height: renderTarget.height
+                width: backingTarget.width,
+                height: backingTarget.height
             )
             depthTexture?.destroy()
             depthTexture = depthResources.texture
@@ -4565,7 +4663,7 @@ private final class CommittedAnimationEvaluationFailure {
         // Recreating the cache on resize applies the new viewport-derived
         // immutable byte budget. The eviction callback releases every old
         // capture and its dependent texture view before replacement.
-        configureRasterizationCache(width: renderTarget.width, height: renderTarget.height)
+        configureRasterizationCache(width: backingTarget.width, height: backingTarget.height)
         prerasterizedTextures.removeAll(keepingCapacity: true)
         failedRasterizationRenderKeys.removeAll(keepingCapacity: true)
         pendingContentMaskFrameFailure = nil
@@ -4748,11 +4846,14 @@ private final class CommittedAnimationEvaluationFailure {
             )
         )
         renderPass.setPipeline(pipeline)
+        // Pixels, not points: the viewport has to cover the whole canvas
+        // texture, or a HiDPI backing store would only be painted in the
+        // bottom-left corner the logical size happens to occupy.
         renderPass.setViewport(
             x: 0,
             y: 0,
-            width: Float(size.width),
-            height: Float(size.height),
+            width: Float(pixelSize.width),
+            height: Float(pixelSize.height),
             minDepth: 0,
             maxDepth: 1
         )
@@ -5830,12 +5931,14 @@ private final class CommittedAnimationEvaluationFailure {
 
         renderPass.setPipeline(pipeline)
 
-        // Set viewport to full canvas size
+        // Set viewport to full canvas size — in device pixels, which is what
+        // the canvas texture is measured in. The projection above stays in
+        // points, so scene coordinates are unaffected by the backing scale.
         renderPass.setViewport(
             x: 0,
             y: 0,
-            width: Float(size.width),
-            height: Float(size.height),
+            width: Float(pixelSize.width),
+            height: Float(pixelSize.height),
             minDepth: 0,
             maxDepth: 1
         )
@@ -13999,8 +14102,13 @@ private final class CommittedAnimationEvaluationFailure {
             throw .rendererResourcesUnavailable
         }
         let maximumTextureDimension = max(1, Int(device.limits.maxTextureDimension2D))
-        let scaledWidth = logicalSize.width * configuration.contentsScale
-        let scaledHeight = logicalSize.height * configuration.contentsScale
+        // Glyphs are rasterized by Canvas2D into a texture, so unlike geometry
+        // they don't sharpen for free on a HiDPI backing store — they have to
+        // be drawn at that scale. The layer's own contentsScale still applies
+        // on top of the renderer's, the way an oversampled layer would.
+        let effectiveScale = configuration.contentsScale * contentsScale
+        let scaledWidth = logicalSize.width * effectiveScale
+        let scaledHeight = logicalSize.height * effectiveScale
         guard scaledWidth.isFinite,
               scaledHeight.isFinite,
               scaledWidth > 0,
@@ -14062,6 +14170,7 @@ private final class CommittedAnimationEvaluationFailure {
             text: text,
             width: pixelWidth,
             height: pixelHeight,
+            contentsScale: effectiveScale,
             configuration: configuration
         )
 
@@ -14087,15 +14196,17 @@ private final class CommittedAnimationEvaluationFailure {
             // Keeping the text texture transparent prevents translucent backgrounds
             // from being composited twice.
             _ = ctx.clearRect!(0, 0, pixelWidth, pixelHeight)
+            // Everything below lays the text out in points; this maps those
+            // points onto the (possibly larger) pixel grid allocated above.
             _ = ctx.scale!(
-                Double(configuration.contentsScale),
-                Double(configuration.contentsScale)
+                Double(effectiveScale),
+                Double(effectiveScale)
             )
 
-            // Set font
-            ctx.font = .string(
-                "\(configuration.fontSize)px \(configuration.cssFontFamily)"
-            )
+            // Set font. Uses the full shorthand (style + weight + family list)
+            // rather than treating the PostScript name as a CSS family, which
+            // silently fell back to a different-metrics font.
+            ctx.font = .string(configuration.cssFont)
 
             // Set text color
             let color = configuration.foregroundRGBA
@@ -14271,6 +14382,7 @@ private final class CommittedAnimationEvaluationFailure {
         text: String,
         width: Int,
         height: Int,
+        contentsScale: CGFloat,
         configuration: CATextRenderConfiguration
     ) -> TextTextureCacheKey {
         let color = configuration.foregroundRGBA
@@ -14279,7 +14391,7 @@ private final class CommittedAnimationEvaluationFailure {
             pixelWidth: width,
             pixelHeight: height,
             fontSize: configuration.fontSize,
-            contentsScale: configuration.contentsScale,
+            contentsScale: contentsScale,
             alignmentMode: configuration.alignmentMode,
             truncationMode: configuration.truncationMode,
             fontFamily: configuration.fontFamily,
@@ -15011,8 +15123,8 @@ private final class CommittedAnimationEvaluationFailure {
             } else {
                 resources = ShadowLayerResources(
                     device: device,
-                    width: Int(size.width),
-                    height: Int(size.height),
+                    width: Int(pixelSize.width),
+                    height: Int(pixelSize.height),
                     format: preferredFormat
                 )
                 shadowLayerResources[shadowRenderKey] = resources
@@ -15298,7 +15410,10 @@ private final class CommittedAnimationEvaluationFailure {
         let previousStencilValue = currentStencilValue
 
         shadowCaptureRootLayer = layer
-        renderTargetSizeOverride = size
+        // The silhouette is captured into a viewport-sized texture, which is
+        // allocated in pixels — so the override that clips and scissors it is
+        // in pixels too.
+        renderTargetSizeOverride = pixelSize
         clipRectStack.removeAll(keepingCapacity: true)
         opacityStack.removeAll(keepingCapacity: true)
         maskNestingDepth = 0
@@ -15436,8 +15551,8 @@ private final class CommittedAnimationEvaluationFailure {
             } else {
                 resources = FilterLayerResources(
                     device: device,
-                    width: Int(size.width),
-                    height: Int(size.height),
+                    width: Int(pixelSize.width),
+                    height: Int(pixelSize.height),
                     format: preferredFormat
                 )
                 filterLayerResources[filteredRenderKey] = resources
@@ -16285,8 +16400,8 @@ private final class CommittedAnimationEvaluationFailure {
         let filterResources = resources.contentMaskResources(
             at: contentMaskIndex,
             device: device,
-            width: Int(size.width),
-            height: Int(size.height),
+            width: Int(pixelSize.width),
+            height: Int(pixelSize.height),
             format: preferredFormat
         )
         guard renderRawCompositionContentMask(
@@ -16649,8 +16764,8 @@ private final class CommittedAnimationEvaluationFailure {
                 } else {
                     resources = CompositionLayerResources(
                         device: device,
-                        width: Int(size.width),
-                        height: Int(size.height),
+                        width: Int(pixelSize.width),
+                        height: Int(pixelSize.height),
                         format: preferredFormat
                     )
                     compositionLayerResources[key] = resources
diff --git a/Sources/OpenCoreAnimation/Rendering/CATextRenderConfiguration.swift b/Sources/OpenCoreAnimation/Rendering/CATextRenderConfiguration.swift
index bd9518f..6a225c9 100644
--- a/Sources/OpenCoreAnimation/Rendering/CATextRenderConfiguration.swift
+++ b/Sources/OpenCoreAnimation/Rendering/CATextRenderConfiguration.swift
@@ -1,4 +1,7 @@
 import OpenFoundation
+#if arch(wasm32)
+import JavaScriptKit
+#endif
 
 /// Describes why a text layer cannot become renderer-independent input.
 internal enum CATextRenderConfigurationError: Error, Equatable, Sendable {
@@ -17,6 +20,10 @@ internal struct CATextRenderConfiguration: Equatable, Sendable {
     let text: String
     let fontFamily: String
     let cssFontFamily: String
+    /// The full CSS `font` shorthand (style + weight + size + families). Font
+    /// names arrive as PostScript names ("HelveticaNeue-Bold"), which are not
+    /// CSS families, so the weight/style has to be split out — see CATextMetrics.
+    let cssFont: String
     let fontSize: CGFloat
     let contentsScale: CGFloat
     let foregroundRGBA: SIMD4<Float>
@@ -79,6 +86,7 @@ internal struct CATextRenderConfiguration: Equatable, Sendable {
         self.text = text
         self.fontFamily = fontFamily
         self.cssFontFamily = Self.cssFontFamily(from: fontFamily)
+        self.cssFont = CATextMetrics.cssFont(name: fontFamily, size: layer.fontSize)
         self.fontSize = layer.fontSize
         self.contentsScale = layer.contentsScale
         self.foregroundRGBA = SIMD4(
@@ -109,3 +117,178 @@ internal struct CATextRenderConfiguration: Equatable, Sendable {
         return "\"\(escaped)\""
     }
 }
+
+// MARK: - Font resolution and measurement
+
+/// Turns Core Animation / SpriteKit font names into CSS font declarations, and
+/// measures text with Canvas2D.
+///
+/// SpriteKit names fonts the way Apple does — by PostScript name, e.g.
+/// "HelveticaNeue-Bold" or "Baskerville-Italic". Those are *not* CSS families:
+/// the family is "Helvetica Neue" and bold/italic are separate properties.
+/// Passing the PostScript name through as a family silently falls back to a
+/// default font whose metrics differ, so text renders at the wrong width and
+/// overflows (or gets truncated inside) its layer.
+///
+/// Measurement lives here too so the metrics used for layout are produced by the
+/// exact same font string the renderer draws with.
+public enum CATextMetrics {
+
+    /// The CSS `font` shorthand for a font name at a size, e.g.
+    /// `HelveticaNeue-Bold` @ 15 →
+    /// `700 15.0px "Helvetica Neue", "HelveticaNeue-Bold", sans-serif`.
+    public static func cssFont(name: String?, size: CGFloat) -> String {
+        let trimmed = (name ?? "").trimmingCharacters(in: .whitespacesAndNewlines)
+        guard !trimmed.isEmpty else { return "\(size)px sans-serif" }
+        if genericFamilies.contains(trimmed.lowercased()) {
+            return "\(size)px \(trimmed.lowercased())"
+        }
+
+        let traits = parse(trimmed)
+        var declaration = ""
+        if traits.isItalic { declaration += "italic " }
+        if let weight = traits.weight { declaration += "\(weight) " }
+        declaration += "\(size)px "
+
+        // Derived family first; the original name second (some engines resolve
+        // PostScript names directly); a generic last so text always renders.
+        var families = ["\"\(escaped(traits.family))\""]
+        if traits.family != trimmed { families.append("\"\(escaped(trimmed))\"") }
+        families.append("sans-serif")
+        return declaration + families.joined(separator: ", ")
+    }
+
+    /// The rendered width of `text`, measured with Canvas2D using the same font
+    /// string the renderer draws with.
+    ///
+    /// Returns `nil` when measurement isn't available (non-browser builds) so
+    /// callers can fall back to an estimate.
+    public static func measuredWidth(
+        _ text: String,
+        fontName: String?,
+        fontSize: CGFloat
+    ) -> CGFloat? {
+        #if arch(wasm32)
+        guard !text.isEmpty else { return 0 }
+        let font = cssFont(name: fontName, size: fontSize)
+        let key = font + "\u{1}" + text
+        if let cached = widthCache[key] { return cached }
+        guard let context = measurementContext() else {
+            if diagnosticBudget > 0 {
+                diagnosticBudget -= 1
+                print("[MEASURE] no 2d context — falling back to estimate")
+            }
+            return nil
+        }
+        context.font = .string(font)
+        let metrics = context.measureText!(text)
+        guard let width = metrics.width.number, width.isFinite, width >= 0 else {
+            if diagnosticBudget > 0 {
+                diagnosticBudget -= 1
+                print("[MEASURE] measureText gave no width | font=\(font) text=\(text.prefix(40))")
+            }
+            return nil
+        }
+        // Only log the weapon-button strings; the HUD measures dozens of labels
+        // and a plain budget gets eaten by the nav tabs before these appear.
+        if diagnosticBudget > 0, text.contains("· move ") || text.contains("swap ⇄") {
+            diagnosticBudget -= 1
+            print("[MEASURE] \(Int(width))px | font=\(font) | chars=\(text.count) | \(text.prefix(48))")
+        }
+        // Layout churn (rebuilding a scrolling list every frame) can push a lot
+        // of distinct strings through here; drop the cache rather than grow it
+        // without bound.
+        if widthCache.count > 4096 { widthCache.removeAll(keepingCapacity: true) }
+        let measured = CGFloat(width)
+        widthCache[key] = measured
+        return measured
+        #else
+        return nil
+        #endif
+    }
+
+    // MARK: - Name parsing
+
+    private struct Traits {
+        let family: String
+        let weight: Int?
+        let isItalic: Bool
+    }
+
+    private static func parse(_ name: String) -> Traits {
+        let parts = name.split(separator: "-", omittingEmptySubsequences: true).map(String.init)
+        var weight: Int?
+        var isItalic = false
+        for trait in parts.dropFirst() {
+            let lowered = trait.lowercased()
+            if lowered.contains("italic") || lowered.contains("oblique") { isItalic = true }
+            if let value = weightValue(in: lowered) { weight = value }
+        }
+        return Traits(
+            family: spacedWords(parts.first ?? name),
+            weight: weight,
+            isItalic: isItalic
+        )
+    }
+
+    /// Compound names are checked before their substrings so "semibold" doesn't
+    /// match as "bold" and "extralight" doesn't match as "light".
+    private static func weightValue(in token: String) -> Int? {
+        let table: [(name: String, weight: Int)] = [
+            ("ultralight", 200), ("extralight", 200),
+            ("semibold", 600), ("demibold", 600),
+            ("extrabold", 800), ("ultrabold", 800),
+            ("thin", 100), ("light", 300),
+            ("regular", 400), ("roman", 400), ("normal", 400), ("book", 400),
+            ("medium", 500), ("bold", 700),
+            ("black", 900), ("heavy", 900),
+        ]
+        for entry in table where token.contains(entry.name) { return entry.weight }
+        return nil
+    }
+
+    /// "HelveticaNeue" → "Helvetica Neue"; single words are left alone.
+    private static func spacedWords(_ value: String) -> String {
+        var result = ""
+        var previousWasLower = false
+        for character in value {
+            if character.isUppercase && previousWasLower { result.append(" ") }
+            result.append(character)
+            previousWasLower = character.isLowercase || character.isNumber
+        }
+        return result
+    }
+
+    private static func escaped(_ value: String) -> String {
+        value
+            .replacingOccurrences(of: "\\", with: "\\\\")
+            .replacingOccurrences(of: "\"", with: "\\\"")
+    }
+
+    private static let genericFamilies: Set<String> = [
+        "serif", "sans-serif", "monospace", "cursive", "fantasy",
+        "system-ui", "ui-serif", "ui-sans-serif", "ui-monospace",
+        "ui-rounded", "math", "emoji", "fangsong",
+    ]
+
+    // MARK: - Measurement context (browser only)
+
+    #if arch(wasm32)
+    nonisolated(unsafe) private static var cachedContext: JSObject?
+    nonisolated(unsafe) private static var widthCache: [String: CGFloat] = [:]
+    /// TEMP: logs the first few measurements so a browser run can confirm the
+    /// Canvas2D path is actually used (and with which font string).
+    nonisolated(unsafe) private static var diagnosticBudget = 8
+
+    /// A detached 2D context kept for the process lifetime; measuring needs no
+    /// pixels, so the canvas is never sized or attached to the document.
+    private static func measurementContext() -> JSObject? {
+        if let cachedContext { return cachedContext }
+        let document = JSObject.global.document
+        let canvas = document.createElement("canvas")
+        guard let context = canvas.getContext("2d").object else { return nil }
+        cachedContext = context
+        return context
+    }
+    #endif
+}
